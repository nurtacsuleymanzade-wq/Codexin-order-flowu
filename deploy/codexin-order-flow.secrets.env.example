# Copy to /etc/codexin-order-flow/secrets.env and chmod 600.
# Keep the real bot token out of git, the browser bundle and API responses.
CODEXIN_TELEGRAM_ENABLED=true
CODEXIN_TELEGRAM_BOT_TOKEN=
CODEXIN_TELEGRAM_CHAT_ID=
